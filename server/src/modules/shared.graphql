type User {
  email: String!
  name: String!
  id: String!
  agency: Agency
}

type Error {
  message: String!
  path: String!
}

type Success {
  message: String!
}

type Property {
  id: String!
  ua_id: String!
  domain: String!
  service_name: String!
  agency: Agency
}

type Agency {
  id: String!
  name: String!
  emailHosts: [String]
}
