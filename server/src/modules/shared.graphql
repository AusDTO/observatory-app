type User {
  email: String
  id: String
}

type Error {
  message: String!
  path: String!
}

type Success {
  message: String!
}

type Property {
  id: String!
  ua_id: String!
  domain: String!
  service_name: String!
  agency: Agency
}

type Agency {
  id: String!
  name: String!
  emailHost: String!
}
